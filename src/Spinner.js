import spinner from "./icons/weather/Pendulum.gif";

const Spinner = () => {
    return (
        <div id="spinner-tag">
            <img src={spinner} alt="Spinner" />
        </div>
    )
}

export default Spinner;